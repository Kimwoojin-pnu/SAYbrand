[build]
builder = "NIXPACKS"

[deploy]
startCommand = "celery -A backend.workers.celery_app worker --loglevel=info --concurrency=4 -B"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "saybrand-worker"
